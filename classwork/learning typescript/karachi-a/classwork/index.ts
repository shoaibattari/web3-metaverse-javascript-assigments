// let myname: string| null;
// myname = 'ssss';
// console.log(myname);

// myname = 123 // error


// if else short hand this is ternary operator
// let newAge = Math.random() > 0.6 ? 'khan' : 60;
 
// if (newAge === "khan"){
//     newAge.toLowerCase()
// }

// if(typeof newAge === 'string'){
//     newAge.toUpperCase()
// }

// typeof newAge === 'string'
// ? newAge.toUpperCase()
// : newAge.toFixed();

// let age: number | 'died' | "unknown";

// age = 90;
// age = 'died'
// age = 'unknown'

// age ='patanhai' // error

// let zia: "zia";
// zia = "zia";
// zia = 'khan' // error



// let yname = Math.random() > 0.7 ? 'shoaib' : undefined;

// if(yname){
//     yname.toUpperCase()
// }

// yname.toUpperCase() //error
// yname?.toUpperCase();


// type RowData = boolean | number | string | null | undefined;

// let data: RowData;




let p = {
    name: 'abc',
    income:30000
}

// let d:{name: string} = p

// let d:{name: string, insome: number, age:number} = p









